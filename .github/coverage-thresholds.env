# Informational coverage thresholds for optional coverage-linux-console job.
# These values DO NOT fail CI; they are used for trend warnings in step summary.
# Adjust upward as coverage maturity improves.

LINE_COVERAGE_MIN=0.00
BRANCH_COVERAGE_MIN=0.00
